<div class="row">
	<div class="col-xs-3">
		<h2>User info</h2>
		<%= render 'users/profile', user: @user %>

		<h2>New book</h2>
		<%= render 'books/form', book: @book %>
	</div>

	<div class="col-xs-9">
		<h2>Books</h2>
		<!--books一覧 -->
		<table class="table table-hover table-inverse">
			<thead>
				<tr>
					<th></th>
					<th>Title</th>
					<th>Opinion</th>
					<th></th>
					<th></th>
					<th colspan="3"></th>
				</tr>
			</thead>

			<tbody>
				<% @books.each do |book| %>
					<tr>
						<td>
							<%= link_to user_path(book.user) do %>
								<%= attachment_image_tag(book.user, :profile_image, :fill, 50, 50, fallback: "no-image-mini.jpg") %>
							<% end %>
						</td>
						<td><%= link_to book.title, book_path(book), class: "book_#{book.id}" %></td>
						<td><%= book.body %></td>
						
						<% if current_user.favorited_by?(book.id) %>
  						<td><%= link_to 'いいね', destroy_favorite_path(book), method: :DELETE %></td>
						<% else %>
  						<td><%= link_to 'いいね', create_favorite_path(book), method: :POST %></td>
						<% end %>
						
						
					</tr>
				<% end %>
				

				
				
<td><%= post.title %></td>
<% if current_user.favorited_by?(post.id) %>
  <td>いいね外す</td>
<% else %>
  <td>いいねする</td>
<% end %>
			</tbody>
		</table>
		<!--books一覧 -->
	</div>
</div>




<li>
  <i class="fa fa-comment"></i>
  <a href="#comments"><%= @post_image.post_comments.count %></a>
</li>

:
:

<div class="comment-list bt style-2" id="comments">
  <div class="float-wrapper">
    <h2 class="comment-count">コメント数:<%= @post_image.post_comments.count %></h2>
  </div>
  
  
  
